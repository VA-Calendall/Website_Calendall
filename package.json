{
  "name": "website_calendall",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cp CNAME dist/ && git subtree push --prefix dist origin gh-pages"
  },
  "devDependencies": {
    "sass": "^1.57.0",
    "vite": "^3.2.3"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
